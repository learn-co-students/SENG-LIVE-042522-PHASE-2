function ProjectForm({projects, name}) {

  // props is an object {projects: Array(11), name: 'aysan'}

  // props.projects
  // props.name

  // const { projects, name } = props

  return (
    <div>ProjectForm</div>
  )
}

export default ProjectForm